function smallestOfTwo(nums) {

    // return nums.sort((a, b) => a - b).slice(0, 2).join(' ');
    
}
nums => nums.sort((a, b) => a - b).slice(0, 2).join(' ');

console.log(smallestOfTwo([30, 15, 50, 5]));